<%- include('header') %>
<% if (error && error.length > 0) { %>
    <div class="alert alert-danger text-center mt -5"><%= error[0] %></div>
<% } %>
<% if (success && success.length > 0) { %>
    <div class="alert alert-success text-center mt-5"><%= success[0] %></div>
<% } %>